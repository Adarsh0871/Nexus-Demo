<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.example</groupId>
  <artifactId>Nexus-Demo</artifactId>
  <version>1.0.0</version>
  <packaging>war</packaging>

  <properties>
    <!-- Define project properties -->
    <java.version>1.8</java.version> <!-- Example property for Java version -->
    <maven.deploy.plugin.version>2.8.2</maven.deploy.plugin.version> <!-- Deploy plugin version -->
  </properties>

  <distributionManagement>
    <repository>
      <id>jenkins</id>
      <url>http://localhost:9091/repository/WebApp/</url>
    </repository>
  </distributionManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>${maven.deploy.plugin.version}</version> <!-- Using property -->
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!-- Sonar Maven Plugin -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>sonar-maven-plugin</artifactId>
        <version>3.2</version>
      </plugin>

      <!-- Maven Deploy Plugin -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>${maven.deploy.plugin.version}</version> <!-- Using property -->
      </plugin>

      <!-- Maven WAR Plugin -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.3.2</version> <!-- Latest compatible version -->
        <configuration>
          <!-- Add any necessary configurations here -->
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
